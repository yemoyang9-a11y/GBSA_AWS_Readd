# Dependencies
node_modules/
backend/node_modules/
frontend/node_modules/

# Build output
dist/
build/
backend/dist/
frontend/dist/
frontend/build/

# Environment variables
.env
.env.local
.env.*.local
backend/.env
frontend/.env

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Testing
coverage/
.nyc_output/

# Temporary files
tmp/
temp/
*.tmp

# AWS
.aws/

# Database
*.sqlite
*.db
.gstack/

# 파이프라인 생성물 — 검수 전 산출물이라 커밋 대상 아님
backend/data/generated/

# 로컬 작업 메모 (세션 간 인계용, git 미포함)
CLAUDE.local.md

# DB 덤프 — 파생 데이터이고 용량이 크다. 저장소에 넣지 않는다
dump/
*.dump

# AWS 이탈 백업 일체 (2026-08-29~30)
# ⚠️ 디렉터리 통째로 제외한다. DB 덤프뿐 아니라 AWS 구성 JSON도 들어가는데,
#    거기엔 계정 ID·인스턴스 ID·VPC/서브넷 ID·공인 IP가 그대로 들어 있다.
#    저장소를 포트폴리오로 공개할 것이므로 파일 단위로 걸러서는 안 된다.
backup/

# impeccable 탐지기용 디자인 컨텍스트 미러 (생성물, 정본은 루트)
frontend/DESIGN.md
frontend/.impeccable/

# Claude Code 로컬 작업 공간 (git worktree 체크아웃 사본 등)
.claude/
