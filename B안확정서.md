# 리캡 생성 방식 — B안 확정 및 구현 계획

**버전** v0.3 · **기준** FR/NFR v0.5, Use Case v0.3, User Flow v0.3, MVP v0.5, 8/17·8/18 팀 결정

## v0.3 변경 사항 (8/18)

| # | 항목 | v0.2 | v0.3 |
| --- | --- | --- | --- |
| 1 | 리캡 저장 | 저장된 위치 + 시연 위치를 사전 생성 | **세션 종료 시 1건 자동 생성.** 발표용 장치가 아니라 상시 동작 구조 |
| 2 | 세션 | 개념 없음 | **마지막 조작으로부터 무조작 30분** |
| 3 | 리캡 주 경로 | 이어보기 → 싸비 리캡 | **브리핑 화면** (대시보드 → 브리핑 → 읽기 화면) |
| 4 | 런타임 LLM 호출 | 리캡 1종 | **리캡·챗봇 2종** (챗봇 P2 → P0) |
| 5 | 배경지식 | 싸비 탭 | **대시보드 표지 `i` 팝업.** 읽는 중 배경 질의는 챗봇이 받음 |
| 6 | 싸비 탭 | 5개 | **3개** (리캡·인물 관계도·챗봇) |
| 7 | 릴리스 게이트 | 24개 / 정답셋 종속 8개 | **30개 / 종속 10개** (8/18 점검분 포함) |

> **1번이 성격 변화다.** v0.2의 8절은 "발표 중 실시간 호출을 없애기 위한 장치"였다. v0.3에서는 **모든 사용자에게 상시 적용되는 동작**이 된다. 리캡이 유일한 대기 발생 지점인데, 재개하며 처음 보는 화면(브리핑)에서 몇 초를 기다리게 하면 재개 경험이 그 자리에서 무너지기 때문이다.
>
> **4번이 구조 변화다.** v0.2는 "리캡만 LLM을 부른다"를 전제로 짜였다. 챗봇이 P0으로 올라오면서 이 전제가 깨진다. 챗봇의 근거 검색 구조는 **아키텍처 문서**에서 설계한다.
>

---

## 1. 결정 사항

**장 단위 사전 생성 + 요청 시 실시간 종합** 방식으로 확정한다.

```
사전(배치)   장별 요약을 미리 생성해 DB 저장
요청 시      기준점 이하 장 요약 + 현재 장의 기준점까지 원문
             → LLM 1회 호출 → 하나의 흐르는 리캡
세션 종료 시  그 시점 기준점의 리캡을 자동 생성해 저장 (다음 접속용 1건)
```

---

## 2. A안·C안을 택하지 않은 이유

| 안 | 방식 | 탈락 사유 |
| --- | --- | --- |
| A | 요청 시 원문 전체에서 바로 생성 | 진도 42% 시점 원문이 23만~29만 토큰(추정)으로 컨텍스트 한계 초과 |
| C | 진도 구간별 요약을 전부 사전 생성 | **장 중간에서 멈추는 경우를 처리할 수 없음.** 타깃 페르소나와 정면 충돌 |
| **B** | 장 요약 사전 생성 + 요청 시 종합 | 저장 부담은 C 수준, 유연성은 A 수준 |

C안 탈락이 핵심이다. 우리 서비스의 존재 이유가 "장 중간에서 멈춘 독자"인데, C안은 그 사람을 못 다룬다. 문제정의서 v0.3의 페르소나("한 번에 20분 남짓, 장 중간에서 멈추는 일이 잦다")가 이 결정을 그대로 지지한다.

---

## 3. 진도 기준점

```
진도 기준점 = 현재 열려 있는 페이지의 직전 페이지 끝
```

넘긴 페이지는 읽은 것으로, 열려 있는 페이지는 읽지 않은 것으로 본다. 독자가 페이지 안에서 어디까지 읽었는지는 알 수 없지만, **넘김이라는 행동이 경계를 만들어준다.**

| 상황 | 기준점 |
| --- | --- |
| 42페이지 열림 | 41페이지 끝 |
| 목차로 80페이지 이동 | 79페이지 끝 |
| 80페이지에서 20페이지로 이동 | 19페이지 끝 |
| 페이지 내 스크롤만 | 변화 없음 |

**건너뛰기를 추론하지 않는다.** 독자가 어디까지 읽었는지 판정하려 들지 않고, 보고 있는 화면을 기준으로 그 앞을 정리한다. 규칙이 하나로 유지되고, 완독 후 재독이나 시연 중 오조작도 자동 복구된다.

**표시 값도 같은 기준을 쓴다.** 대시보드 진도 바·브리핑 진도 바·싸비 기준점이 모두 현재 위치에서 나온다. 최대 도달 값(watermark)을 따로 두지 않는다(FR-BRF-005 🚦).

> **읽기 화면에는 진도 바를 두지 않는다.** 고정 페이지 방식에서 진도는 페이지 경계에서만 갱신되므로, 연속적인 위치를 암시하는 막대가 실제 동작과 맞지 않는다. 페이지 번호("12 / 340")만 표시한다.
>

---

## 4. 세션

```
세션 종료 = 마지막 조작으로부터 무조작 30분 경과
```

| 상황 | 동작 |
| --- | --- |
| 무조작 30분 **경과** 후 재진입 | 새 세션. **브리핑 화면**을 거친다 |
| 무조작 30분 **이내** 재진입 | 같은 세션. 브리핑을 건너뛰고 읽던 페이지로 직행 |

30분 이내에 돌아온 사용자는 이탈한 적이 없으므로 되살릴 기억도 없다. **브리핑이 뜨는 조건과 리캡이 준비되어 있는 조건이 같아지므로, 브리핑에서 LLM 호출이 발생하지 않는다.**

> 세션 종료 감지의 구현 방식은 **아키텍처 문서**에서 다룬다. 이 문서는 판정 기준(무조작 30분)만 규정한다.
>

---

## 5. 동작 구조

**런타임 LLM 호출은 리캡과 챗봇 두 종류다.**

| 기능 | 요청 시 동작 | LLM 호출 |
| --- | --- | --- |
| 리캡 (브리핑) | 세션 종료 시 저장해 둔 결과 표시 | **0회** |
| 리캡 (싸비 탭) | 기준점이 저장분과 같으면 재사용, 다르면 종합 | **1회** (재사용 시 0회) |
| **챗봇** | 기준점 이하 원문·엔티티 + 배경지식에서 근거 검색 → 답변 | **1회 / 질의** |
| 인물 관계도 | `WHERE 등장페이지 <= 기준점` | 0회 |
| 인물 맥락 설명 | `WHERE 등장페이지 <= 기준점` | 0회 |
| 타임라인 | `WHERE 발생페이지 <= 기준점` | 0회 |
| 용어 설명 | `WHERE 등장페이지 <= 기준점` | 0회 |
| 배경지식·책 소개 | 고정 텍스트 조회 (대시보드 `i` 팝업) | 0회 |

인물·관계·타임라인·용어는 **진도별 스냅샷을 따로 만들 필요가 없다.** 각 항목에 등장 페이지만 붙여두면 조회 시점에 쿼리가 걸러낸다. 사전 생성은 도서당 1회면 된다.

리캡이 종합을 필요로 하는 이유는 **누적 서사**라서다. 잘라낼 수도, 단순히 이어붙일 수도 없다.

챗봇은 성격이 다르다. 질문이 임의로 들어오므로 근거를 **찾아야** 한다. 조회형처럼 고정 쿼리로 끝나지 않는다.

> **챗봇의 근거 검색 구조는 이 문서 범위 밖이다.** 아키텍처 문서에서 설계한다. 여기서 규정하는 것은 근거의 **범위**뿐이다(8절).
>

---

## 6. 리캡 종합 호출

```
독자가 42페이지를 열어둔 상태
장 구성: 3장 = 28~40페이지, 4장 = 41~55페이지

기준점 = 41페이지 끝

입력   [1~3장 요약]  +  [4장 시작(41페이지) ~ 41페이지 끝 원문]
       + "앞 내용에 이어지도록 하나의 흐르는 줄거리로 정리"

출력   완성된 리캡 (마지막 문단이 41페이지 내용과 연결)
```

장 경계에 정확히 걸린 경우에도 **호출은 1회 발생한다.** 장별 요약을 그냥 이어붙이면 토막난 글이 되기 때문이다.

**투입 조건**
- 장 요약: `장.종료페이지 <= 기준점` 인 것만
- 현재 장 원문: 장 시작 페이지부터 기준점 페이지까지

---

## 7. 리캡 저장과 재사용

### 7.1 무엇을 저장하는가

**"다음 접속 때 보여줄 리캡 1건"이다.** 모든 장이나 모든 페이지의 리캡을 미리 만들어 두는 것이 아니다.

```
사용자가 40페이지까지 읽고 앱을 닫음
        ↓ 무조작 30분 경과 → 세션 종료
시스템이 기준점(39페이지 끝) 리캡을 자동 생성해 저장
        ↓ 며칠 뒤 재진입
브리핑 화면이 저장분을 대기 없이 표시
```

| 항목 | 규칙 |
| --- | --- |
| 생성 시점 | 세션 종료 (무조작 30분) |
| 저장 단위 | **사용자·도서당 1건.** 이후 세션 종료 시 갱신 |
| 생성 조건 | 기준점이 전체의 10% 미만이면 생성하지 않음 |
| 재사용 조건 | 저장분의 기준점과 요청 기준점이 **일치할 때만** |

### 7.2 언제 실시간으로 만드는가

기준점이 저장분과 달라진 요청, 즉 **읽는 중 싸비 리캡 탭**이다.

```
브리핑에서 리캡@39 확인 → 40페이지 진입
싸비 리캡 열기          → 기준점 39. 저장분과 일치 → 재사용 (호출 0회)
62페이지까지 읽음
싸비 리캡 열기          → 기준점 61. 불일치 → 실시간 생성 (호출 1회)
같은 62페이지에서 다시 열기 → 세션 동안 들고 있던 결과 재사용 (호출 0회)
```

**실시간 생성분은 영구 저장하지 않는다.** 세션 동안만 유지하고 세션이 끝나면 버린다. 세션 종료 시 만들어지는 저장분과 슬롯이 다르다.

> **왜 페이지 단위로 갈랐는가** — 장 단위로 재사용하면 같은 장 안에서 몇 페이지 더 읽고 리캡을 열었을 때 방금 읽은 내용이 빠진 채 "여기까지 정리"로 표시된다. 기준점이 다르면 새로 만드는 쪽이 표시 내용과 실제 위치를 일치시킨다.
>

### 7.3 발표 안정성

시연 위치의 저장분은 **관리자가 미리 넣어둔다.** 정상 동선(브리핑 진입)이 저장분을 타므로 발표 중 실시간 호출이 발생하지 않는다.

- 지연·Throttling 위험 제거
- 검수 통과한 문장만 노출 → 환각 리스크 제거
- 리허설과 본 발표 결과가 동일 → 발표 대본 작성 가능

> Bedrock의 프롬프트 캐싱과는 다른 개념. 생성 결과 자체를 DB에 저장해두는 것이다.
>
> **NFR-PERF-002(리캡 첫 텍스트 3초)는 저장분을 비운 상태로 별도 측정해야 한다.** 시연 경로로만 재면 게이트가 자동 통과되면서 아무것도 검증하지 못한다. 저장분 재사용 경로는 NFR-PERF-003(1.5초)이 담당한다.
>

---

## 8. 스포일러 차단 지점

방어 층위가 기능군별로 다르다.

**리캡 — LLM 입력 자체를 자른다**
- 장 요약: `장.종료페이지 <= 기준점` 인 것만 투입
- 현재 장 원문: 기준점 페이지에서 절단해서 투입
- 프롬프트 지시에 의존하지 않는다. 애초에 들어가지 않는다

**챗봇 — 검색 범위를 자른다**

근거 범위를 셋으로 나눈다.

| 근거 | 진도 상한 |
| --- | --- |
| 기준점까지의 원문·장 요약 | **적용** |
| 기준점까지의 인물·관계·용어·사건 | **적용** |
| 배경지식 | **미적용** (1페이지 시점에도 안전한 고정 콘텐츠) |

**근거를 찾지 못하면 항상 같은 문구를 반환한다.** 대상이 기준점 이후에 등장하는지, 작품에 존재하지 않는지를 **판별하지 않는다.** 판별하면 그 결과가 응답 차이로 새어나간다.

> 응답 문구: "현재까지 읽은 페이지 기준으로 알 수 없는 내용입니다. 다른 질문 해주세요."

**인젝션 방어의 실질은 검색 범위 강제다.** 사용자 입력으로 검색 범위가 바뀌지 않으므로, 인젝션에 성공해도 LLM이 볼 수 있는 것은 기준점 이하 데이터뿐이다. 프롬프트 방어는 2차선이다(NFR-SEC-006 🚦).

**차단만으로는 부족하다 (8/18 점검).** 위 세 장치는 전부 막는 방향이라, 모든 질의를 안내 문구로 답하는 구현이 게이트를 통과한다. **FR-QNA-007**(오탐 방지, 읽은 구간 근거가 있는 질의는 거절되지 않을 것)을 004와 쌍으로 판정한다. 그리고 이 검증이 성립하려면 검색 로그가 남아야 하므로 **NFR-OBS-005**(챗봇 질의 로깅)가 함께 신설됐다 — FR-QNA-006과 NFR-SEC-006은 게이트인데 v0.5까지 그 로그를 남기라는 조항이 없었다.

**조회형 기능 — 조회 시 상한**
- `WHERE 위치페이지 <= 기준점`
- 검색 필터링 문제가 아니라 어느 데이터를 내려줄지 고르는 문제

**공통 — 검수**
- 사전 생성물은 배포 전 검수를 거친 것만 사용

**적용 범위** — 차단 대상은 **싸비가 제공하는 정보**다. 본문 접근은 제한하지 않으며, 목차로 어느 장이든 이동할 수 있다. **배경지식은 상한 대상이 아니다.**

---

## 9. 필요한 데이터

| 데이터 | 필수 필드 | 비고 |
| --- | --- | --- |
| **페이지** | 페이지 번호, 본문 텍스트 | 고정 단위. 기기·폰트와 무관하게 동일 텍스트 |
| **장 경계** | 장 번호, 장 제목, 시작 페이지, 종료 페이지 | **B안의 전제.** 기준점 → 장 매핑 |
| 장별 요약 | 장 번호, 요약문 | 검수 완료본만 사용 |
| 인물 | 별칭 목록, 최초 등장 페이지 | |
| 관계 | 인물 A·B, 관계 라벨, 확립 페이지 | |
| 타임라인 | 사건, 발생 페이지 | |
| 용어 | 용어, 설명, 최초 등장 페이지 | |
| 배경지식·책 소개 | 도서 ID, 구분(배경지식 / 소개), 텍스트 | **분리 저장.** 대시보드 `i` 팝업에서 영역을 나눠 표시 |
| **독서 위치** | 사용자 ID, 도서 ID, 마지막으로 열어본 페이지 | 서버 저장 |
| **세션** | 사용자 ID, 도서 ID, 마지막 조작 시각, 리캡 생성 여부 | **v0.3 신설.** 무조작 30분 판정과 배치 대상 선별에 쓰임. 요구사항 대응분은 **FR-DAT-011**(8/18 점검 신설) |
| **저장된 리캡** | 사용자 ID, 도서 ID, 기준점 페이지, 리캡 텍스트 | **사용자·도서당 1건.** 세션 종료 시 갱신 |

> 위치를 페이지 번호로 통일했다. 페이지가 고정 단위이므로 폰트나 화면 크기가 달라져도 같은 페이지는 같은 텍스트를 담는다. 오프셋이나 %로 다시 환산할 필요가 없다.
>
> **저장된 리캡이 사용자별로 바뀐 것이 v0.3의 변화다.** v0.2는 도서 단위 사전 생성이었으나, 세션 종료 시점이 사용자마다 다르므로 사용자·도서 조합으로 잡아야 한다.
>

---

## 10. 비용

「탁류」 46만 자 기준 추정치다.

| 항목 | 추정 |
| --- | --- |
| 사전 생성 전체 (요약·인물·관계·타임라인·용어) | 도서 1권당 약 6,000원 |
| 리캡 종합 1회 | 약 100원 |
| 리허설 포함 리캡 200회 호출 | 약 2만 원 |
| **챗봇 질의 1회** | **미추정** |

**데모 규모에서 비용은 의사결정 요인이 아니다.** 전체가 5만 원을 넘지 않으므로 비용보다 품질 기준으로 모델을 고른다.

> **챗봇 승격으로 런타임 호출이 하나 늘었으나 재추정하지 않았다.** 근거 검색 구조가 정해지지 않아 입력 토큰 규모를 알 수 없기 때문이다. 아키텍처 확정 후 채운다. 다만 위 결론(비용은 의사결정 요인이 아니다)이 뒤집힐 규모는 아닐 것으로 본다.
>

---

## 11. 정답셋 구축 방식

원문 전수 조사 대신 **복수 모델 교차 검증**을 쓴다.

```
1단계   모델 3종에 별칭·관계 추출을 각각 요청
2단계   결과를 대조해 불일치 항목만 추출
3단계   불일치 항목만 원문으로 대조해 확정
```

**대상** — 인물 별칭(주요 인물 10인 이상), 오탐 검증셋 30건, 관계 라벨, 리캡 검수 5건과 판정 기준, **챗봇 응답 검증셋 — 환각 판정(FR-QNA-002)과 오탐 검증셋 30건(FR-QNA-007)**

**기록** — 교차 검증 실행 기록, 불일치 항목의 확정 근거

> **한계** — 모델들이 같은 실수를 하면 이 방식으로 잡히지 않는다. 한국어 호칭·가족관계 처리가 공통 취약 영역이므로, 주요 인물의 별칭만큼은 불일치 여부와 무관하게 한 번 훑어보는 편이 안전하다.
>
> **챗봇 승격으로 검증 대상이 늘었다.** 스포일러 리그레션 30건에 챗봇 케이스가 추가되고, FR-QNA-002(답변 환각 ≤ 5%)와 FR-QNA-004(응답 문구 동일)가 새 게이트다.
>

---

## 12. 구현 순서

| 단계 | 작업 | 산출물 |
| --- | --- | --- |
| 1 | **Bedrock 모델 접근 권한 신청** | 승인 소요 시간 있음. 먼저 착수 |
| 2 | **아키텍처 문서 작성** | 챗봇 근거 검색 구조 · 인젝션 방어 수단 · 세션 종료 감지 구현 · 리캡 저장소 구조 |
| 3 | 「탁류」 원문 확보, 페이지 분할 | 페이지 테이블 |
| 4 | 장 구성 파악 | 장 경계 테이블 |
| 5 | 장별 요약 배치 생성 | 장 요약 테이블 |
| 6 | 인물·관계·타임라인·용어·배경지식 배치 생성 | 각 테이블 (페이지 필드 포함) |
| 7 | 정답셋 구축 (교차 검증 + 불일치 대조) | 정답셋 · 검증 기록 |
| 8 | 검수 | 검수 기록 |
| 9 | 리캡 종합 API 구현 | 요청 시 LLM 1회 |
| 10 | **세션 종료 배치 구현** | 무조작 30분 판정 → 리캡 자동 생성·저장 |
| 11 | **브리핑 화면 구현** | 저장분 표시 · 목차상 위치 · 진도 바 · 마저 읽기 |
| 12 | **챗봇 구현** | 근거 검색 + 응답. 근거 부재 시 통일 문구. **질의 로깅 포함**(NFR-OBS-005) |
| 13 | 시연 위치 리캡 사전 생성 | 발표용 저장 결과 |

> **2번이 9·12번을 막는다.** 챗봇 검색 구조가 정해지지 않으면 12번에 착수할 수 없고, 리캡 저장소 구조가 정해지지 않으면 10번이 흔들린다.
>

---

## 13. 남은 결정 사항

| 항목 | 비고 |
| --- | --- |
| **정답셋 구축 담당·기한** | 8/22 Freeze 전 완료 필요. **릴리스 게이트 30개 중 10개가 여기 종속** |
| 작업별 모델 선정 | 요약·인물 추출·관계 추출·리캡 종합·**챗봇 응답** |
| Bedrock Throttling·리전·할당량 대응 | |
| 페이지 분할 단위 | 한 페이지당 글자 수 기준 |
| 장 첫 페이지에 기준점이 걸릴 때 연결성 처리 | FR-RCP-003 |
| NFR-SEC-006 판정 기준 | 아키텍처 확정 후 채운다. 게이트이므로 비어 있으면 판정 불가 |

---

## 14. FR/NFR 대응

| 요구사항 | 대응 |
| --- | --- |
| FR-BRF-001~005 | 브리핑 화면 — 4절(세션), 7절(저장분 표시) |
| FR-DAT-001 | 장 경계 테이블 (9절) |
| FR-DAT-002 | 페이지 분할 (9절) |
| FR-DAT-003 | 장별 요약 (12절 5단계) |
| FR-DAT-009 | 세션 종료 시 리캡 사전 생성 (7.1절) |
| FR-DAT-010 | 저장 리캡 재사용 범위 (7.2절) |
| FR-RCP-004 | 장 중간 지점 대응 (6절) |
| FR-QNA-001~005 | 챗봇 동작·통일 응답 (5·8절) |
| FR-QNA-006 | 챗봇 근거 범위 (8절) |
| FR-SPL-002 | 조회 시 상한 (8절) |
| FR-SPL-003 | 생성 입력 상한 (8절) |
| NFR-PERF-002 | 별도 측정 시나리오 (7.3절 주석) |
| NFR-PERF-003 | 저장분 재사용 경로 (7.2절) |
| NFR-SEC-006 | 검색 범위 강제 (8절). **판정 기준은 아키텍처 확정 후** |
| FR-QNA-007 | 챗봇 오탐 방지 (8절) |
| NFR-OBS-005 | 챗봇 질의 로깅 (8절) |
| FR-DAT-011 | 세션 엔티티 (9절) |
| NFR-AI-004 | 데이터 선택 단계 차단 (8절) |
| NFR-AI-008 | 교차 검증 방식 (11절) |
| NFR-AI-016 | 사전 생성 실패 시 폴백 (7.1절) |
| NFR-AI-017 | 런타임 호출 2종 (5절) |
