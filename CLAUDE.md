# CLAUDE.md — 싸비 (Reading Recap)

> 이 파일은 모든 Claude Code 세션에 로드된다. **여기 있는 규칙은 어떤 작업에서도 예외 없다.**
> 상세는 각 문서로 넘긴다 — 아래 8장 문서 지도 참조.

---

## 1. 이 프로젝트가 하는 일

전자책 독자를 위한 **진도 인식 사이드바(싸비)**. 독자가 **읽은 지점까지만** 아는 상태로 줄거리 리캡·인물 관계도·챗봇을 제공한다.

**이 시스템의 존재 이유는 "읽지 않은 부분을 절대 노출하지 않는 것"이다.** 기능이 잘 동작해도 스포일러가 한 건 새면 실패다. 릴리스 게이트 29개 중 절반 이상이 이 하나에 걸려 있다.

데모 — 채만식 「탁류」 1권, 태블릿 PC 우선 반응형 웹앱.

---

## 2. 절대 규칙 — 어기면 게이트 실패

| # | 금지 | 조항 |
| --- | --- | --- |
| 1 | 조회·조립 함수에 **cutoff 인자 없이** 저장소 접근 | FR-SPL-002 🚦 |
| 2 | 기준점 결정기 **밖에서** cutoff 또는 `%` 계산 (프론트 포함) | FR-BRF-005 🚦 |
| 3 | **프롬프트 지시로 상한 걸기** — 상한은 데이터 선택 단계에서만 | R4 불변식, NFR-AI-004 🚦 |
| 4 | LLM 게이트웨이(⑥)를 거치지 않는 직접 호출 | 4.3절 |
| 5 | 공개 콘텐츠 스토어에 파이프라인(⑦) 외의 쓰기 | 4.3절 |
| 6 | 상한 필터를 **우회하는 폴백 경로** — 실패 = 미노출이 원칙 | FR-SPL-005 🚦 |
| 7 | 기준점 초과 여부를 **"판별"하는 코드** (단, **완독**은 예외 — 아래 부연) | FR-QNA-004 🚦 |
| 8 | 클라이언트가 **"기준점"을 직접 보내는 API** | 3.3절 |
| 9 | 페이지 내 스크롤로 서버 이벤트 발생 | FR-PRG-003 AC④ |
| 10 | 프론트에서 **페이지 재분할** (폰트·화면 크기 변화는 스크롤 길이만 바꾼다) | FR-PRG-001 |

**7번 부연** — "이건 아직 안 읽은 내용이니 숨기자"고 판단하는 코드를 만들지 않는다. 시스템 어느 경로도 기준점 초과 데이터에 **접근하지 않으므로** 판별할 능력 자체가 없어야 한다. 그게 이 조항이 성립하는 방식이다.

**7번 완독 예외 (D14, 2026-08-27 팀 결정)** — 유일한 예외는 **완독**(`is_complete`)이다. 마지막 페이지까지 읽으면 K가 책 전체를 덮으므로 초과분이라는 개념 자체가 없고, 따라서 판별할 초과 데이터도 없다. 이때만 챗봇이 근거에 없는 대상을 "이 책에 나오지 않아요"라고 단정할 수 있다. `is_complete`는 **기준점 결정기가 파생하는 값**이며(다른 파생값과 동일 취급 — 절대 규칙 2번), 그 밖의 어느 코드도 `page >= total_pages`를 재계산하지 않는다. 완독이 아닌 모든 상태에서는 7번·R10이 그대로다.

---

## 3. 시스템의 축 — 기준점

저장값은 **"마지막으로 열어본 페이지" 하나**다. 나머지는 전부 파생값이다.

```
저장값:  reading_position.current_page

파생 (기준점 결정기 전용 함수, ② 독서 상태 서비스 안):
    cutoff      = 진도 레코드 없음 ? 0 : current_page   ← 스포일러 상한 K
    percent     = current_page / total_pages
    is_complete = 진도 레코드 있음 && current_page >= total_pages   ← 완독. 챗봇 완독 예외의 유일한 근거 (D14)
    chapter     = current_page가 속한 장

인터페이스: getCutoffSnapshot(deviceId, bookId)
         -> { current_page, cutoff, percent, is_complete, chapter }
```

**모든 조회·조립·검색은 이 스냅샷의 cutoff만 상한으로 쓴다.** 각 요청은 시작 시 스냅샷을 1회 받아, 요청 내 모든 쿼리·조립·로그가 그 값을 쓴다.

**기준점을 움직일 수 있는 입력은 "페이지 이동" 하나뿐이다.** 챗봇 질문 텍스트, 쿼리 파라미터, 프론트 계산값 — 어떤 것도 관여하지 않는다.

---

## 4. 시스템 불변식 R1~R12

**모든 설계·구현 판단은 이 목록에 대해 검증한다.**

| # | 불변식 | 조항 |
| --- | --- | --- |
| R1 | **기준점 = 마지막으로 열어본 페이지.** 책을 한 번도 열지 않았을 때(진도 레코드 없음)만 0. 페이지 이동 시 즉시 갱신, 앞뒤 모두, 스크롤 무영향 | FR-PRG-003 🚦 |
| R2 | **표시 파생값은 단일 원천에서 나온다.** 최대 도달 값(watermark)은 존재하지 않는다 | FR-BRF-005 🚦 |
| R3 | **본문 접근은 제한하지 않는다.** 차단 대상은 싸비가 제공하는 정보뿐. 목차는 전체 상시 노출 | FR-SPL-001, FR-NAV-001 |
| R4 | **상한 강제 위치는 데이터 선택 단계다.** 조회형 = 쿼리 조건, 리캡 = 입력 절단, 챗봇 = 근거 조립 범위 | FR-SPL-002·003 🚦 |
| R5 | **배경지식은 상한 대상이 아니다.** 1페이지 시점에도 안전한 고정 콘텐츠 | FR-BGK-002 🚦 |
| R6 | **세션 = 마지막 조작으로부터 무조작 30분.** 브리핑은 새 세션 진입 시 1회 경유 | FR-DAT-009, FR-BRF-001 |
| R7 | **저장 리캡은 사용자·도서당 1건.** 세션 종료 시 진도와 무관하게 생성 | FR-DAT-009 |
| R8 | **재사용은 기준점 완전 일치 시에만.** 실시간 생성분은 영구 저장하지 않는다 | FR-DAT-010 |
| R9 | **런타임 LLM 호출은 리캡·챗봇 2종뿐.** 인물·관계·용어·사건·배경지식은 전부 DB 조회(0회) | NFR-AI-017 |
| R10 | **챗봇은 근거 부재 시 항상 같은 문구.** 이유를 판별하지 않는다. **예외: 완독(`is_complete`)** — 이때는 근거에 없는 대상을 "이 책에 나오지 않아요"로 단정할 수 있다 (D14). 완독 판정은 기준점 결정기 파생값 하나뿐 | FR-QNA-004 🚦 |
| R11 | **어떤 실패에서도 초과 노출 금지.** '마저 읽기'는 리캡과 독립 동작 | FR-SPL-005 🚦 |
| R12 | **검수 통과분만 공개.** 미검수 도서는 진입 가능 상태로 노출되지 않는다 | FR-ADM-005 🚦 |

---

## 5. 용어

| 용어 | 뜻 |
| --- | --- |
| **싸비** | 읽기 화면 옆의 사이드바 패널. 3탭 — 리캡 · 인물 관계도 · 챗봇 |
| **기준점 / cutoff / K** | 스포일러 상한. `현재 페이지`이며, 진도 레코드가 없을 때만 `0`. 코드에서 `K`로 표기 |
| **완독 (is_complete)** | 진도 레코드가 있고 `현재 페이지 ≥ 마지막 페이지`. 기준점 결정기 파생값. 챗봇 R10 완독 예외의 유일한 근거 (D14) |
| **리캡** | 기준점까지의 줄거리 요약. LLM 1회 종합 |
| **브리핑 화면** | 새 세션 진입 시 1회 경유. 저장 리캡 + 목차 위치 + 진도 바 + '마저 읽기' |
| **확립 페이지** | 인물 관계가 드러나는 페이지. 호칭 첫 등장과 서술 노출 중 **이른 쪽** |
| **전량 주입 (B-1)** | 챗봇 근거 방식. 장 요약·엔티티·배경지식 전량 + 검색 청크 6개 |
| **완비 여부 (ssabi_ready)** | 싸비 데이터가 준비된 도서인지 |
| **🚦** | 릴리스 게이트 조항. **총 29개** |

**ID 체계** — `FR-XXX-NNN`(기능) · `NFR-XXX-NNN`(비기능) · `UC-NN`(유스케이스) · `R1~R12`(불변식) · `A1~A11`(설계 제기 항목) · `D1~D13`(8/19 팀 결정) · `D14`(8/27 팀 결정 — 완독 시 챗봇 단정 부정 허용)

---

## 6. 작업 규칙

| 규칙 | 이유 |
| --- | --- |
| **구현 근거를 조항 ID로 주석에 남긴다** — `// FR-SPL-002 🚦` | 나중에 왜 그렇게 짰는지 복원 가능해야 한다 |
| **명세에 없는 것은 지어내지 말고 질문하고 멈춘다** | 미결 항목이 실제로 있다(7장). 빈칸을 그럴듯하게 채우면 잘못된 결정이 코드에 굳는다 |
| **다른 파트의 파일을 수정하지 않는다** | 각자 범위는 개인 명세서에. 남의 계약이 필요하면 스텁을 쓴다 |
| **상한 필터를 "나중에 붙이자"로 미루지 않는다** | 나중에 전부 고쳐야 하고, 그 사이 우회 경로가 남는다 |
| **한 세션에 작업 단위(S1, S2…) 하나** | 컨텍스트가 길어지면 앞의 제약(cutoff 인자, 로그 적재)을 뒤쪽에서 빠뜨린다 |
| **로그 적재는 기능의 일부다** | 로그가 없으면 게이트 판정 자체가 불가능하다 |

### 세션 완료 조건

① 명세서의 수용 기준 충족 ② 테스트 통과 ③ 조항 주석 존재 ④ **로그 레코드가 실제로 쌓이는지 확인**

---

## 7. 테스트 규칙

| 규칙 | 이유 |
| --- | --- |
| **게이트 조항이 걸린 로직은 테스트를 먼저 쓴다** | 구현 후에 쓰면 코드의 동작을 그대로 굳혀서, 상한을 잘못 걸었어도 통과시킨다 |
| 테스트 이름에 **조항 문구를 그대로** 넣는다 | `test("FR-SPL-002: 조회 결과에 기준점 초과 레코드 0건")` |
| **테스트가 실패하면 테스트를 고치지 않는다. 구현을 고친다** | 가장 흔한 실패 모드. 특히 상한 테스트의 assertion을 느슨하게 바꾸는 일 |
| **상한 테스트는 "0건"이다.** "대부분" "거의"로 쓰지 않는다 | 1건도 통과가 아니다 |
| **negative는 반드시 positive와 쌍으로** | K=40에서 안 보이고 **K=130에서는 보이는지** 함께 확인. positive가 없으면 쿼리가 항상 빈 결과를 반환해도 통과한다 |
| 게이트 테스트를 skip·주석 처리하지 않는다 | 그 게이트가 이 프로젝트의 본질이다 |
| **테스트용 우회 경로(상한 무시 플래그)를 만들지 않는다** | 그 경로가 곧 우회 표면이다 |

---

## 8. 문서 지도

| 문서 | 언제 읽나 |
| --- | --- |
| **`dev-spec-00-shared.md`** | 착수 전 1회 — 스키마·API 계약·로그 필드·시드 데이터 |
| **`dev-spec-R{n}-*.md`** | 내 파트 세션마다 — 작업 단위 S1~Sn, 자가 검증, 정지선 |
| **`dev-spec-05-claude-code-and-test.md`** | 테스트 설계 시 · 검수 전 |
| `architecture-r1.md` | 설계 근거가 필요할 때. 절 번호로 참조 |
| `decisions-0819-final.md` | "이건 왜 이렇게 정했나" 확인 시 (D1~D13) |
| `dev-plan-0819.md` | 일정·체크포인트 확인 |

**절 참조 단축키** — 3.3(기준점) · 4.4.1(스위퍼) · 5.1(스키마) · 5.2(세 흐름) · 5.3(챗봇 근거) · 5.4(스포일러 강제·인젝션) · 6장(게이트 책임표)

---

## 9. 미결 — 마주치면 멈추고 물어본다

**아래는 아직 정해지지 않았다. 추론해서 채우지 말 것.**

| 항목 | 상태 |
| --- | --- |
| 챗봇 난이도 판정 규칙 | R3가 구현 시 정함. 조건 2개 — 근거 범위 비관여, 선택 모델 로깅 |
| 데모 시연 시나리오 | 미확정 — 대상 관계·질문·기준점 전후 페이지 모두 |
| 프롬프트 캐싱 (A8-2) | 성능 측정에서 2.0초 초과 시에만 |
| 리캡 모델 | Haiku **잠정**. FR-RCP-001 🚦(환각 0건) 미통과 시 상향 |
| 배치 생성 6종 모델 | 미지정 |

**A10 잔여 위험** — 「탁류」는 공개 도메인 유명 작품이라 모델이 줄거리를 이미 알 수 있다. 근거에서 빼도 자기 기억으로 답할 수 있으며 **구조로 막지 못한다.** 프롬프트 규칙(NFR-AI-005)이 유일한 수단이고 100% 보장이 아니다. **구조적 해결책을 지어내지 말 것.**

**결정됨 — D14 (2026-08-27 팀 결정, 완독 시 챗봇 단정 부정 허용)**
- 마지막 페이지까지 읽은 독자(`is_complete`)에게는 챗봇이 근거에 없는 대상을 "이 책에 나오지 않아요"로 단정할 수 있다 — R10·절대 규칙 7번에 완독 예외를 뒀다.
- `is_complete`는 기준점 결정기(`getCutoffSnapshot`)가 파생하는 값 하나뿐이다. 다른 코드가 `page >= total_pages`를 재계산하면 `derived-value-single-source` 정적 테스트가 잡는다.
- 완독이어도 A10은 유효하다 — "없다"는 판단도 조립된 근거에 근거해야 하고, 모델의 사전 지식으로 「탁류」 내용을 지어내면 안 된다. 프롬프트(`COMPLETION_STANCE`, `chatbot/service.ts`)가 이걸 명시한다.
- 완독이 아닌 모든 상태에서 R10은 그대로다. 완독이어도 모델이 `[NO_EVIDENCE]`를 반환하면 기존대로 고정 문구로 폴백한다.
- ⚠️ 이 저장소에 `decisions-0819-final.md`가 없다 — 외부 결정 문서에 D14 항목을 정식 기록할 것.

---

## 10. 커밋

```
{type}({파트}): {요약} — {조항 ID}

예: feat(R2): 기준점 결정기 — FR-PRG-003
    test(R3): 근거 조립 범위 검증 — FR-QNA-006
    fix(R4): 이력형 관계 최신 라벨 선택 — A6, FR-CHR-001
```

**작업 단위(S) 하나 = 커밋 하나.** CP4 교차 리뷰에서 어느 커밋이 어느 게이트를 건드렸는지 추적할 수 있어야 한다.

---

## 11. 일정

| 시점 | |
| --- | --- |
| 8/19 | 착수 · CP0(계약 동결) · CP1(분할 실측) |
| 8/20 | CP2 — 경로별 단독 동작 |
| 8/21 | CP3 — 관통 · 실데이터 전환 |
| **8/22** | **CP4 — Freeze.** 이후 기능 추가 없음 |
| 8/25 | CP5 — 게이트 판정 1차 |
| 8/27 | CP6 — 리허설 |
| **8/28** | **발표** |
