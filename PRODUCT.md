# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

전자책을 읽다가 자리를 비우고 다시 펼치는 독자. 태블릿 PC를 주 기기로 쓰며, 돌아왔을 때 "여기까지 무슨 일이 있었는지"를 스포일러 없이 파악하고 싶어한다. 인물 관계가 헷갈리거나 궁금한 대목이 생기면 직접 찾아보지 않고 물어서 확인하고 싶어한다.

## Product Purpose

RE:ADD는 전자책을 읽는 화면 자체를 포함한 완결형 리딩 앱으로, 읽은 지점(기준점)까지만 정보를 제공하는 진도 인식 사이드바 "싸비"를 내장한다. 목적은 독자가 스포일러 걱정 없이 언제든 리캡·인물 관계도·챗봇으로 줄거리를 따라잡게 하는 것이다. 성공은 "읽은 페이지를 초과하는 정보가 단 한 건도 노출되지 않으면서, 리캡·챗봇이 실제로 쓸모 있는 답을 주는 것"으로 정의된다 — 기능이 잘 동작해도 스포일러가 한 건이라도 새면 실패로 간주한다.

## Positioning

다른 "스포일러 방지" 기능은 보통 UI 경고나 프롬프트 지시(LLM에게 "이 뒤는 말하지 마")에 기대는 반면, RE:ADD는 상한(cutoff)을 **데이터 선택 단계**에서 구조적으로 강제한다 — 조회·조립·검색 코드 자체가 기준점 이후 데이터에 접근할 능력을 갖지 않는다. "판별해서 숨긴다"가 아니라 "애초에 접근하지 못한다"가 이 시스템이 성립하는 방식이다.

(확인됨) RE:ADD는 사이드바 부가기능·플러그인이 아니라, 리더 화면을 포함해 계속 발전시켜 나갈 독립 읽기 플랫폼이다.

## Operating Context

- 데모 작품: 채만식 「탁류」 1권(공개 도메인 텍스트). 발표일 2026-08-28
- 태블릿 PC 우선 반응형 웹앱
- 새 세션 진입 시 브리핑 화면을 1회 경유한다 — 저장 리캡, 목차 위치, 진도 바, '마저 읽기'로 구성
- 읽기 화면에서 싸비 패널은 기본 닫힘. 우측 상단의 위치 고정 토글 버튼으로 열고 닫는다(패널 내부나 top-bar 안에 두지 않는 이유: 닫히면 버튼도 같이 사라지거나, 열고 닫을 때 버튼이 위아래로 튄다)
- 싸비는 3탭 — 리캡 · 인물 관계도 · 챗봇
- 세션은 마지막 조작으로부터 무조작 30분으로 종료된다
- 페이지 이동마다 진도 이벤트를 발신한다(fire-and-forget). 스크롤은 서버 이벤트를 발생시키지 않는다
- 발표 전 릴리스 게이트 29개를 통과해야 하며, 그중 절반 이상이 스포일러 차단 하나에 걸려 있다

## Capabilities and Constraints

- 저장값은 "마지막으로 열어본 페이지"(`current_page`) 하나뿐이다. cutoff(기준점)·percent·chapter는 전부 이 값에서 파생된다
- 기준점(cutoff) = `current_page − 1`. 이 값을 움직일 수 있는 입력은 "페이지 이동" 하나뿐이며, 챗봇 질문 텍스트나 프론트 계산값은 관여하지 않는다
- 모든 조회·조립·검색은 기준점을 상한으로 **데이터 선택 단계**(쿼리 조건 / 리캡 입력 절단 / 챗봇 근거 조립 범위)에서만 강제한다. 프롬프트 지시로 상한을 거는 것은 금지되어 있다
- 본문(원문) 접근 자체는 제한하지 않는다 — 목차는 항상 전체 노출. 차단 대상은 싸비가 제공하는 파생 정보(리캡·관계도·챗봇 근거)뿐이다
- 런타임 LLM 호출은 리캡·챗봇 2종뿐이다. 인물·관계·용어·사건·배경지식은 전부 사전 처리된 DB 조회로 제공된다(런타임 LLM 호출 0회)
- 저장 리캡은 사용자·도서당 1건이며, 기준점이 완전히 일치할 때만 재사용한다. 실시간 생성분은 영구 저장하지 않는다
- 어떤 실패 상황에서도 "초과 노출 금지"가 최우선 원칙이다 — 상한을 우회하는 폴백 경로를 두지 않으며, 실패 시 기본값은 미노출이다
- 알려진 잔여 위험: 「탁류」는 공개 도메인 유명작이라 LLM이 줄거리를 이미 알고 있을 수 있다. 근거 조립에서 제외해도 모델이 자기 지식으로 답할 가능성이 있으며, 이는 구조로 막을 수 없고 프롬프트 규칙이 유일한(그리고 100% 보장이 아닌) 완화 수단이다
- 접근성(Accessibility) 요구사항은 현재 미정으로 남아 있다(2026-08-21 확인). 확정되면 이 문서를 갱신한다
- 미정 사실(CLAUDE.md 9장 기준, 추론해서 채우지 말 것): 챗봇 난이도 판정 규칙, 데모 시연 시나리오, 프롬프트 캐싱 적용 여부, 리캡 모델 최종 확정(Haiku 잠정), 배치 생성 6종 모델

## Brand Commitments

- 앱 이름: **RE:ADD**(읽기 화면 로고에 사용)
- 사이드바 기능명: **싸비**(리캡·인물 관계도·챗봇 3탭 패널)
- 개발팀명: Team Ssabi

## Evidence on Hand

- 데모 콘텐츠: 채만식 「탁류」 1권(공개 도메인 텍스트)
- 비주얼 시안: Figma에 존재(코드 주석이 특정 프레임을 "시안 47:132" 식으로 직접 참조). `frontend/src/design-tokens.test.ts`가 시안 실측값을 색상·서체·형태 토큰으로 이미 고정해 두었다 — 명조체(Nanum Myeongjo)와 고딕체(Gothic A1), accent `#3b3db2`, 싸비 강조색 `#c86b3d` 등. 이 문서는 그 값을 재정의하지 않는다; 향후 DESIGN.md나 시각 작업은 이 기존 토큰 체계를 근거로 삼는다

## Product Principles

1. 접근 자체가 불가능해야 안전하다 — 판별해서 숨기는 방식이 아니라 접근을 원천 차단하는 방식으로 스포일러를 막는다.
2. 상한은 데이터 선택 단계에서만 강제한다. 프롬프트 지시는 보조 수단일 뿐 강제 수단이 될 수 없다.
3. 저장값은 하나, 나머지는 전부 파생값이다 — 여러 진실 소스(예: watermark)를 만들지 않는다.
4. 실패 시 기본값은 "미노출"이다. 편의를 위해 상한을 우회하는 폴백을 두지 않는다.
5. RE:ADD는 사이드바 부가기능이 아니라, 계속 발전시켜 나갈 완결형 읽기 플랫폼이다.
