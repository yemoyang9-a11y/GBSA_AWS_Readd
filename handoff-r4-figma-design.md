# R4 인수인계 — Figma 디자인 적용 (최종 갱신 2026-08-20 밤)

**선행 문서** `handoff-r4.md`(백엔드·조회 스트림) — 이 문서는 그와 **별개 작업 스트림**이다.
**브랜치** `feature/R4-frontend` (커밋은 로컬에만 있다. **push 하지 않았다.**)

---

## 0. 30초 요약

12개 태스크 중 **10개 완료·리뷰 통과**. 그 위에 사용자 요청으로 **싸비 여닫기 기능**을 추가했다.
남은 것은 **Task 11(인물 관계도, React Flow)** 과 **Task 12(리캡·챗봇 탭)** 두 개다.

`npx tsc --noEmit` 0건, **25 files / 111 tests 전부 통과**(직접 실행해 확인). 워킹 트리 깨끗.

**작업 방식이 중간에 바뀌었다.** Task 9까지는 서브에이전트(구현자→리뷰어)로 진행했고,
Task 10부터는 **사용자 지시로 컨트롤러가 직접 구현**한다(§6). 남은 태스크도 그렇게 이어간다.

**Figma 연동이 살아 있다.** 착수 전 시안 대조가 실제로 재작업을 막아 줬다 — §3을 반드시 읽을 것.

---

## 1. 문서 지도

| 문서 | 내용 |
| --- | --- |
| `.superpowers/sdd/2026-08-20-figma-design-system/progress.md` | **원장** — 태스크별 진행·리뷰 결과·판단 전부. 컨텍스트가 끊기면 여기가 복구 지점 |
| `docs/superpowers/specs/2026-08-20-figma-design-system-design.md` | 설계 스펙 — 토큰 실측값, 시안-계약 불일치 5건 |
| `docs/superpowers/plans/2026-08-20-figma-design-system.md` | 12개 태스크 계획. **일부 값이 실제 시안과 다르다(§3)** |
| `.superpowers/sdd/.../task-N-brief.md` | 태스크별 지시서. **8·9는 시안 실측으로 개정된 판본이다** |

---

## 2. 완료 상태

| # | 태스크 | 커밋 | 상태 |
| --- | --- | --- | --- |
| 1~6 | 토큰·표지·진도바·카드·통계/탭/버튼·헤더 | (이전 세션) | 완료 |
| 7 | 대시보드 조립 | `96a5ba7` | 완료 (이번 세션에 리뷰 통과) |
| 8 | 브리핑 화면 | `c16eaf0` → `6b1ec43` | 완료 (fix 1회) |
| 8R | 브리핑 상단 시안 개정 | `946e23e` | 완료 |
| 9 | 읽기 화면 셸 | `1045b64` | 완료 |
| 10 | 싸비 패널 헤더·탭 | `49df4b6` | 완료 |
| — | 싸비 여닫기 (계획 밖, 사용자 요청) | `bf916f5` `548e9db` `78fa2b5` `343d64c` | 완료 |
| — | `.gstack/` 무시 (보안) | `2016754` | 완료 |
| **11** | **관계도 (React Flow)** | — | **미착수** |
| **12** | **리캡·챗봇 탭** | — | **미착수** |

**리뷰를 받지 않은 것** — Task 10 이후는 컨트롤러 직접 구현이라 독립 리뷰가 없다.
`6b1ec43`(브리핑 저자 fix)도 스코프 재리뷰를 생략했다(사유는 원장 Ruling 참조).
**최종 전체 리뷰 때 이 구간을 함께 봐야 한다.**

---

## 3. ⚠️ 계획을 그대로 믿지 말 것 — 시안과 4번 어긋났다

**착수 전 Figma 조회로 계획 오류를 네 번 잡았다.** 남은 Task 11·12도 같은 위험이 있다.

**Figma 파일 키** `u1rpA2C7aoD7YinAMyY0Tm` (View 시트로 조회 가능. 편집 권한은 조회에 불필요)

| 프레임 | 노드 ID | 비고 |
| --- | --- | --- |
| 대시보드 | `1:476` | |
| 브리핑 | `1:477` | |
| 싸비 - 리캡 (읽기+패널 열림) | `1:480` | 스펙이 "읽기 화면"으로 잘못 참조 |
| **싸비없는 책읽기** | `47:132` | **스펙이 참조한 적 없음.** 읽기 셸의 실제 대상 |
| 싸비 - 인물관계도 | `48:1067` | **Task 11 대상 — 착수 전 대조할 것** |
| 싸비 - 챗봇 | `79:1254` | **Task 12 대상 — 착수 전 대조할 것** |
| 책 간략 정보 | `47:189` | |

실제로 잡아낸 불일치: 브리핑 상단 구조 전체(Task 8R 재작업 발생), 읽기 화면 top-bar
높이 80→72·본문 폭 640→560·17→18px·로고 배치, 싸비 탭이 밑줄→**알약형**·13→12px.

**조회 방법** — `get_metadata`로 구조를 보고 `get_design_context`로 값을 받는다.
`get_design_context` 전에 `skill://figma/figma-design-to-code/SKILL.md`를 먼저 읽어야 한다(도구가 요구).
페이지 전체(`0:1`) 조회는 출력이 커서 잘리니 프레임 단위로 볼 것.

---

## 4. 남은 태스크에서 예상되는 문제

### Task 11 — 관계도
- **`@xyflow/react`가 jsdom에서 렌더되는지 미검증.** 패키지 설치와 `dist/style.css` 존재,
  vitest가 CSS import를 무시한다는 점은 확인됐다. 실제 렌더는 붙여봐야 안다.
  안 되면 좌표 계산을 순수 함수로 분리해 DOM 없이 검증하는 우회로가 계획에 있다.
- 간선 라벨은 **색이 아니라 글자로 병기**한다 (NFR-USE-006).
- 이력형 관계는 서버가 최신 1개만 주므로 **프론트에서 고르지 않는다** (절대 규칙 7번).

### Task 12 — 리캡·챗봇 탭
- 시안의 floating 버튼 텍스트가 **"추기 질문하기"** 다. 오타로 보이며 **실제 문구를 사용자에게 확인해야 한다.**
- 탭 콘텐츠 실측(시안 `97:1246`): Gothic A1 Regular **15px**, `muted`, `leading-[1.6]`.

### 공통
- 시안에 있으나 **만들면 안 되는 것**: 브리핑 목차의 '읽기' 버튼(FR-BRF-004·D12 🚦),
  싸비의 **타임라인 탭**(G8). 둘 다 게이트/합의 위반이다.

---

## 5. 백로그 — 사용자가 "Task 12 이후 추가 기능 작업"으로 미룬 것

**싸비 탭 기억과 무의미한 조회 차단을 함께 만족시키기.**
현재는 패널을 닫을 때 언마운트한다. 숨기기만 하면 닫힌 패널의 리캡 탭이 살아 있어
페이지를 넘길 때마다 **보이지 않는 리캡 스트리밍**이 나가고, 그건 LLM 재호출이자
분당 3회 상한 위반이다(NFR-AI-017). 대가로 닫았다 열면 탭이 기본값으로 돌아간다.

둘 다 만족시키려면 패널을 유지(숨김)하되 `panelOpen`을 조회 조건에 넣어야 한다. 손댈 곳:
- `Reader.tsx`의 리캡 스트리밍 effect — `tab !== 'recap'` 조건에 `|| !panelOpen` 추가
- `useSsabiData`의 그래프 조회 — 닫힘일 때 조회하지 않도록
- `handleAsk`(챗봇)는 사용자 입력이 있어야 발화하므로 영향 없음

**주의** — 숨김 방식으로 바꾸면 현재 토글 테스트(`queryByRole('tab')` 부재 단언)가 깨진다.
접근성 트리에서도 빠지도록 `hidden` 속성을 쓰고 단언을 그에 맞게 바꿔야 한다.
`Reader.tsx`의 `panelOpen` 선언부에 TODO 주석으로도 남겼다.

---

## 6. 작업 방식 (중간에 바뀜)

- **Task 9까지**: 서브에이전트 기반. 태스크마다 새 구현자(Sonnet) → 별개 리뷰어 → 지적 시 수정.
- **Task 10부터**: **사용자 지시로 서브에이전트 사용 중단.** 로컬에서 셸 창이 반복적으로
  뜨는 것이 문제가 됐다(vitest가 테스트 파일마다 워커 프로세스를 띄우는 것이 근본 원인).
  컨트롤러가 직접 구현하고, **셸 호출을 최소화**한다 — 파일 읽기·편집은 셸을 쓰지 않는
  도구로, 검증과 커밋은 한 번씩 묶어서.
- Figma 조회는 셸을 쓰지 않으므로 대조는 계속 수행한다.

---

## 7. 사용자가 내린 결정 (내 판단을 뒤집은 것 포함)

| 결정 | 내용 |
| --- | --- |
| "N일 만이에요" | **만든다.** 내가 "데이터 없으니 만들지 않는다"고 했으나 사용자가 뒤집었다. `MOCK_DAYS_SINCE_LABEL = '3일 만이에요'` 고정값 + TODO 주석. 실데이터 필드가 생기면 교체 |
| RE:ADD 로고 | **텍스트로 통일.** 시안은 PNG 에셋이나 대시보드 `Header.tsx`가 이미 텍스트다 |
| 진도 바 색 | **브리핑만 보라(accent).** 대시보드는 검정 유지 — `ProgressBar`에 `tone` prop, 기본값 `'ink'` |
| 싸비 기본 상태 | **닫힘.** 내가 "열림"으로 뒀으나 사용자가 뒤집었다. 토글을 눌러야 열린다 |
| 싸비 토글 위치 | **고정.** 상태에 따라 버튼이 튀던 것을 지적받아 절대 위치로 고정 |
| `.gitignore` | **커밋함.** 보안상 필요(§8) |

---

## 8. 보안 점검 결과 (사용자 요청)

- `.gstack/`(gstack browse 산출물)에 **`terminal-internal-token`** 등이 있다.
- **git 히스토리 확인: `.gstack`도 `terminal-internal-token`도 커밋된 적이 없다.** 안전.
- 무시 규칙이 워킹 트리에만 있어 되돌려지면 보호가 사라지는 상태였다 → `2016754`로 커밋해 영구화.
- 이번 세션에 **push·force push·삭제·reset·clean은 하지 않았다.** 되돌릴 수 없는 작업 없음.

---

## 9. 재개 방법

```bash
cd C:/Users/yemoy/aws-project/reading-recap
git branch --show-current                                          # feature/R4-frontend
cat .superpowers/sdd/2026-08-20-figma-design-system/progress.md    # 원장 = 복구 지점
cd frontend && npx vite --port 5199 --strictPort                   # dev 서버 (mock 모드)
```

**낡은 dev 서버는 새 Tailwind 토큰을 모른다.** `bg-ssabi` 같은 클래스에서 컴파일 에러가
나면 코드 버그가 아니라 서버를 새로 띄우면 되는 경우다. 실제로 한 번 오진할 뻔했다.

`npm test`는 반드시 `frontend/`에서 실행한다. 루트에서 돌리면 설정을 못 읽는다.

---

## 10. 확인하지 못한 것 (추측과 사실을 구분한다)

- **Task 10 이후 구현은 독립 리뷰를 받지 않았다.** 테스트 통과와 tsc 클린은 직접 확인한
  사실이지만, 코드 품질·스펙 준수의 제3자 검토는 없다.
- **태블릿 가로(1133×744) 기준 확인을 하지 않았다.** 스펙이 정한 기준 뷰포트가 그것인데
  브라우저 확인은 더 넓은 폭에서 했다.
- **React Flow의 jsdom 렌더는 여전히 미검증**(§4).
- **실제 백엔드와 통신해 본 적이 없다.** 전부 `VITE_USE_MOCK=true` 기준이다.
- 시안의 floating 챗봇 버튼(`48:840`)은 텍스트가 **"챗봇버튼임"** 이다 — 시안 자체가 미완성이다.
- **토글 버튼 위치가 두 값에 의존하는데 연동되지 않는다.** `top-24`(96px) = top-bar 72px +
  패널 헤더 pt-6 24px. 두 값이 다른 파일에 있어, 한쪽을 바꾸면 버튼 위치가 어긋난다.
