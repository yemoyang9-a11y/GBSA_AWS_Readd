# R4 인수인계 — Figma 디자인 적용 (2026-08-20)

**선행 문서** `handoff-r4.md`(백엔드·조회 스트림) — 이 문서는 그와 **별개 작업 스트림**이다.
**브랜치** `feature/R4-frontend` (origin 에 push 됨, 이후 커밋은 로컬만)

---

## 0. 30초 요약

Figma 시안의 시각 디자인을 프론트엔드에 입히는 12개 태스크 계획 중 **7개 구현 완료, 6개 리뷰 완료**. 대시보드까지 시안대로 렌더되는 것을 브라우저로 확인했다. Task 7(대시보드)은 커밋됐으나 **리뷰를 받지 않았다** — 여기서 이어받으면 된다.

작업은 서브에이전트 기반으로 진행했다: 태스크마다 새 구현자(Sonnet) → diff 리뷰 → 지적 시 수정 → 범위 좁힌 재리뷰. 전 과정 기록이 `.superpowers/sdd/2026-08-20-figma-design-system/progress.md`(원장)에 있다. **컨텍스트가 끊겨도 이 원장이 복구 지점이다.**

---

## 1. 문서 지도 (이 순서로 읽는다)

| 문서 | 내용 |
| --- | --- |
| `docs/superpowers/specs/2026-08-20-figma-design-system-design.md` | 설계 스펙 — 토큰 실측값, 컴포넌트 인벤토리, 4화면 조립, 시안-계약 불일치 6건 |
| `docs/superpowers/plans/2026-08-20-figma-design-system.md` | 구현 계획 12개 태스크 (각 태스크에 완성 코드·테스트 포함) |
| `.superpowers/sdd/2026-08-20-figma-design-system/progress.md` | **원장** — 태스크별 진행·리뷰 결과·내가 내린 판단 전부 |
| `.superpowers/sdd/.../task-N-brief.md` · `task-N-report.md` | 태스크별 지시서와 구현자 보고서 |

원장의 `Ruling:` 줄이 **내가 사용자 대신 내린 결정**이다. 이어받는 사람은 그 목록을 먼저 훑어야 한다(§6).

---

## 2. 완료 상태

| # | 태스크 | 커밋 | 상태 |
| --- | --- | --- | --- |
| 1 | 디자인 토큰·웹폰트·전역 base | `b8f3f25` | 완료 (수정 1회) |
| 2 | TypographicCover (타이포그래픽 표지) | `ebddfe3` | 완료 |
| 3 | ProgressBar | `21027a3` | 완료 |
| 4 | BookCard·BookGrid | `d5e597f` | 완료 (수정 1회) |
| 5 | StatCard·FilterTabs·Button | `7fb8620` | 완료 (1건 parked) |
| 6 | Header (nav-bar) | `ccac365` | 완료 |
| 7 | 대시보드 조립 | `96a5ba7` | **구현만 완료 — 리뷰 안 받음** |
| 8 | 브리핑 화면 | — | 미착수 (계획 수정 완료, §4) |
| 9 | 읽기 화면 셸 | — | 미착수 |
| 10 | 싸비 패널 셸 | — | 미착수 |
| 11 | 관계도 (React Flow) | — | 미착수 |
| 12 | 리캡·챗봇 탭 | — | 미착수 |

**미커밋 변경 1건** — `docs/superpowers/plans/2026-08-20-figma-design-system.md` (Task 8 결함 수정, §4). 커밋해도 되고 그대로 이어받아도 된다.

---

## 3. 검증 — 직접 실행해 확인한 것

```bash
cd frontend && npx tsc --noEmit    # 0건
cd frontend && npm test            # 25 files / 104 tests 통과
```

**둘 다 내가 직접 실행해 확인했다** (Task 7 커밋 `96a5ba7` 기준, 2026-08-20 17:41).

화면도 브라우저로 직접 봤다 — 대시보드가 시안대로 렌더된다: 오프화이트 배경, 흰 카드 + 둥근 모서리 + 옅은 그림자, 「탁류」명조 조판 표지, 통계 카드 2개(파란 숫자), 알약형 필터 탭, 진도 바 70%.

### ⚠️ dev 서버를 새로 띄워야 한다

**Tailwind config 를 바꾸기 전에 띄운 dev 서버는 `bg-canvas` 토큰을 모른다.** 낡은 서버는 다음 에러를 띄우며 화면이 통째로 죽는다:

```
[plugin:vite:css] [postcss] index.css:1:1:
The `bg-canvas` class does not exist.
```

**이건 코드 버그가 아니다.** 새 서버를 띄우면 정상이다. 실제로 이것 때문에 한 번 오진할 뻔했고, 깨끗한 포트로 새로 띄워 확인해 코드가 멀쩡함을 확정했다.

```bash
cd frontend && npx vite --port 5199 --strictPort
```

작업 중 띄운 서버가 **5173·5175·5199 세 개 남아 있을 수 있다.** 5173·5175 는 낡았으니 죽이고 쓰는 게 안전하다.

---

## 4. 이어받는 사람이 바로 할 일

### 4.1 Task 7 리뷰 (가장 먼저)

`96a5ba7` 이 커밋됐지만 리뷰를 못 받았다. 구현자가 보고서를 쓰는 중에 중단됐으므로 **`task-7-report.md` 가 없다.** 리뷰를 돌리려면 보고서 없이 diff 만으로 진행하거나, 리뷰를 생략하고 Task 8 로 넘어간 뒤 마지막 전체 리뷰에서 함께 보면 된다.

```bash
SDD=~/.claude/plugins/cache/claude-plugins-official/superpowers/6.3.0/skills/subagent-driven-development
bash "$SDD/scripts/review-package" docs/superpowers/plans/2026-08-20-figma-design-system.md ccac365 96a5ba7
```

**확인하지 못한 것** — Task 7 구현자에게 "브라우저로 보고 무엇이 보이는지 서술하라"고 지시했으나 그 서술을 받지 못했다. 다만 **내가 직접 브라우저로 확인**했으므로 화면 동작 자체는 사실이다.

### 4.2 Task 8~12 순차 진행

계획대로 진행한다. 브리프는 **12개 전부 미리 추출돼 있다**(`task-N-brief.md`).

---

## 5. 알려진 문제 — 우선순위 순

### 5.1 🔴 도서 카드에 제목이 두 번 보인다 (사용자 판단 필요)

조판 표지가 제목을 크게 보여주고, 바로 아래 카드 정보에 같은 제목이 또 나온다. 스크린샷으로 확인된 **시각적 중복**이다.

원인은 설계 결정의 부작용이다. 시안은 표지가 **이미지**여서 제목이 한 번만 보이는데, 「탁류」는 공개도메인 고전이라 표지 이미지가 없어 타이포그래픽 표지로 대체했다(사용자 승인). 그 결과 제목이 두 번 나온다.

접근성 쪽은 이미 고쳤다 — 조판 표지에 `aria-hidden="true"` 를 넣어 스크린리더는 제목을 한 번만 읽는다. **남은 건 시각적 판단이다.** 선택지: (a) 그대로 둔다 (b) 카드 정보 블록에서 제목을 빼고 저자만 남긴다 (c) 표지에 제목 대신 다른 것을 넣는다. **사용자 결정 사항이다.**

### 5.2 🟡 nav-bar 좌측이 비어 보인다

시안은 좌측에 22px 명조 제목("수진님의 서재") + 13px 부제 두 줄인데, 사용자 지시로 제목 줄을 제거해 부제 한 줄만 남았다. 스크린샷상 좌측 여백이 크다. 기능 문제는 아니고 의도된 제거지만, 균형이 어색하면 부제 크기를 키우는 등의 조정 여지가 있다.

### 5.3 🟡 시안에 있으나 계약에 없는 것 6건

스펙 §7 에 표로 정리돼 있다. 요약: 통계·필터는 프론트 집계로 처리(완독은 데이터가 없어 **항상 0**), 도서 검색은 비활성, 표지는 타이포그래픽, 카드 소개 문구는 `intro_summary` 필드 확정 전까지 비움, 브리핑의 "N일 만이에요"는 만들지 않음(마지막 방문 시각을 주는 엔드포인트가 없다).

### 5.4 🟡 parked — `text-white` 토큰 논쟁

Task 5 리뷰가 `text-white` 를 토큰 위반으로 잡았으나 **그대로 두기로 판단**했다. 근거는 원장의 해당 Ruling 참조. 요지는 어두운 버튼 위 흰 글씨와 카드 배경(`surface`)이 hex 만 같을 뿐 역할이 다르며, 한 토큰으로 묶으면 카드 배경을 바꿀 때 버튼 글씨까지 따라 바뀐다는 것이다.

---

## 6. 내가 사용자 대신 내린 판단 (전부 원장에 근거와 함께 기록)

되돌리고 싶으면 원장의 해당 `Ruling:` 줄을 보면 된다.

| # | 판단 | 틀렸을 때 비용 |
| --- | --- | --- |
| 1 | G11 "임의 값 금지"를 **색에만** 적용 (글자 크기·간격은 허용) | 토큰 승격 작업 추가 |
| 2 | Task 7 테스트의 다중 매치 결함을 실행 전 수정 | 없음 (원안은 확정 실패) |
| 3 | `intro_summary` 타입 캐스트 허용 (계약 미확정 필드) | D-3 확정 시 정리 1회 |
| 4 | Task 1 `allowJs` → `tailwind.config.d.ts` 로 교체 | 라운드 1회 |
| 5 | 커밋 메시지 조항 ID 없으면 **비워둔다** (스펙 절 번호를 그 자리에 넣지 않음) | 표기만 달라짐 |
| 6 | 조판 표지 접근성 중복을 **즉시** 수정 (deferred 하지 않음) | 라운드 1회 |
| 7 | `text-white` 유지 (§5.4) | 2곳 교체, 수 분 |
| 8 | Task 9 의 중복 테스트 취소 (기존 테스트가 이미 검증) | 없음 |
| 9 | Task 8 의 중복 목차 테스트 취소 (기존 테스트가 이미 검증) | 없음 |
| 10 | `BriefingView` 에 `title`·`author` props 추가 (데이터는 이미 컨테이너에 있었음) | 라운드 1회 |
| 11 | 브리핑의 "N일 만이에요" 만들지 않음 (데이터 없음) | 필드 생기면 한 줄 추가 |

---

## 7. 함정 기록 (전부 실제로 겪은 것)

- **낡은 dev 서버가 토큰을 모른다** — §3 참조. 코드 버그로 오진하기 쉽다.
- **`getByText('읽는 중')` 은 대시보드에서 반드시 실패한다** — 통계 라벨·필터 탭·카드 진도 라벨 세 곳에 같은 문구가 있다. `data-testid` 로 범위를 좁혀야 한다.
- **`findByText` 는 `aria-hidden` 을 무시한다** — 접근성 속성으로 테스트 중복 매치를 못 고친다. 접근성 트리를 보는 건 `getByRole` 계열뿐이다. 이 착각으로 한 번 잘못된 수정을 지시할 뻔했고 리뷰어가 잡았다.
- **계획에 중복 테스트가 두 번 들어 있었다** — Task 8·9 둘 다 기존 테스트와 같은 검증을 추가하라고 했다. 계획을 쓸 때 기존 테스트 목록을 충분히 대조하지 않은 결과다. 남은 태스크도 착수 전에 해당 테스트 파일을 먼저 읽는 게 안전하다.

---

## 8. 확인하지 못한 것 (추측과 사실을 구분한다)

- **Task 7 의 리뷰를 받지 못했다.** 테스트 통과와 화면 렌더는 내가 직접 확인한 사실이지만, 코드 품질·스펙 준수 검토는 없었다.
- **태블릿 가로(1133×744) 기준 확인을 하지 않았다.** 브라우저 확인은 1432px 폭에서 했다. 스펙이 정한 기준 뷰포트는 태블릿 가로다.
- **브리핑·읽기·싸비 화면은 아직 디자인이 입혀지지 않았다.** Task 1~7 은 토큰·공용 컴포넌트·대시보드까지다. 나머지 화면은 여전히 무스타일에 가깝다.
- **React Flow 가 jsdom 에서 렌더되는지는 미검증.** Task 11 의 위험 요소이며, 그래서 좌표 계산을 순수 함수로 분리해 DOM 없이 검증하도록 계획했다. 실제 동작은 붙여봐야 안다.
- `@xyflow/react` 12.11.3 설치와 `dist/style.css` 존재, vitest 가 CSS import 를 무시한다는 점은 **확인했다.**

---

## 9. 재개 방법

```bash
cd C:/Users/yemoy/aws-project/reading-recap
git branch --show-current          # feature/R4-frontend 여야 한다
cat .superpowers/sdd/2026-08-20-figma-design-system/progress.md   # 원장 = 복구 지점
```

원장에서 `Task <N>: complete` 줄이 있는 태스크는 끝난 것이다. 없는 첫 태스크부터 이어간다. 지금 기준으로는 **Task 7 리뷰 또는 Task 8 착수**가 다음이다.
