# 싸비 — Claude Code 운영 · 테스트 규약 · 인간 검수 계획

**근거** `architecture-r1.md` v0.9 · `decisions-0819-final.md`
**전제** 구현은 Claude Code가 주도하고, 사람은 **검수와 판정**을 맡는다.
**선행 필독** `dev-spec-00-shared.md`

---

## 0. 이 문서가 다루는 문제

Claude Code에게 구현과 테스트를 함께 맡기면 **테스트가 코드의 동작을 그대로 굳히는** 실패 모드가 생긴다. 구현이 상한을 잘못 걸었으면 테스트도 그 잘못된 동작을 통과시킨다. 이 시스템은 게이트 29개가 스포일러 차단에 걸려 있어서, 이 실패 모드가 곧 데모 실패다.

그래서 두 가지 구조를 둔다.

1. **게이트 조항이 걸린 로직은 테스트를 먼저 쓴다** — 구현이 아니라 **조항에서** 테스트를 유도한다
2. **사람이 하는 일을 "코드 읽기"가 아니라 "판정"으로 정의한다** — 코드를 다 읽을 시간은 없다

---

## 1. 역할 — Claude Code와 사람

### Claude Code가 하는 것

구현 전체, 테스트 작성, 리팩터링, 로그 적재, 스크립트, 검증 자동화. 각 개인 명세서의 `S1, S2…` 작업 단위가 대략 한 세션 분량이다.

### 사람이 반드시 하는 것 — 위임 불가

문서가 **명시적으로 사람을 요구**하는 항목이다. Claude Code에게 넘기면 조항 위반이다.

| # | 항목 | 조항 근거 | 담당 |
| --- | --- | --- | --- |
| 1 | **검수 6항목 판정** + 수정 내용 기록 | FR-ADM-005 🚦, NFR-AI-011 🚦 | R1 |
| 2 | **별칭 사람 확정** — 호칭·직함·가족관계 유형 (일반명사 오탐) | 2.1 ② | R1 |
| 3 | **관계 확립 페이지 확정** | 2.1 ② | R1 |
| 4 | **배경지식·소개 안전 판정** — 후반부 줄거리 혼입 확인 | FR-BGK-002 🚦, R5 | R1 |
| 5 | **상한 강제 코드 리뷰 + 기록** — "코드 작성자 외 1명" | NFR-SEC-006 판정 ① | 교차 짝 |
| 6 | **챗봇 탐색적 테스트** — "팀이 직접 사용하며" 부당 거절 사례 수집 | 6.4절 | 전원 |
| 7 | **데모 시연 시나리오 선정** | 미확정 (7.1절) | 전원 |
| 8 | 챗봇 난이도 판정 규칙 결정 | 미확정 (D13 ②) | R3 |

> **5번은 해석이 필요하다.** 조항은 "코드 작성자 외 1명"을 요구하는데, 작성자가 Claude Code라면 담당자 본인이 리뷰어가 될 수 있다는 해석도 가능하다. 그러나 담당자는 자기 파트를 이미 여러 번 봤으므로 눈이 굳는다. **교차 리뷰 짝(다른 파트 담당자)이 리뷰하는 것으로 운용**하고, 이 해석을 리뷰 기록에 남긴다. — **[확인 필요]** 팀 합의 사항

### 사람이 하는 것 — 판정

| 시점 | 판정 |
| --- | --- |
| CP1 | 페이지 크기 확정 (실측 3건을 보고) |
| CP2·CP3 | 아래 4장 체크리스트 |
| CP4 | 교차 리뷰 + 기록, 기능 동결 선언 |
| CP5 | 게이트 통과 여부 |
| CP6 | 데모 실행 가부 |

---

## 2. Claude Code 세션 운영

### 2.1 세션 시작 프롬프트 템플릿

```
읽을 것: CLAUDE.md (공통 계약), dev-spec-R{n}-*.md (내 명세서)

이번 세션 작업: S{n} — {작업명}

규칙:
- 구현 근거를 조항 ID로 주석에 남긴다 (예: // FR-SPL-002 🚦)
- 명세서에 없는 것은 지어내지 말고 질문하고 멈춘다
- 다른 파트의 파일을 수정하지 않는다. 필요하면 스텁을 쓴다
- 게이트 조항이 걸린 로직은 테스트를 먼저 쓴다 (3장 규약)

완료 조건: 명세서의 수용 기준 + 테스트 통과 + 로그 레코드 생성 확인
```

### 2.2 세션을 끊는 지점

**한 세션에 S 두 개를 넣지 않는다.** 컨텍스트가 길어지면 앞의 제약(cutoff 인자, 로그 적재)을 잊고 뒤쪽 코드에서 빠뜨린다.

각 세션 끝에서 확인할 것 — ① 수용 기준 충족 ② 테스트 통과 ③ **조항 주석이 붙어 있는가** ④ 로그가 실제로 쌓이는가.

### 2.3 커밋 단위

S 하나 = 커밋 하나. 커밋 메시지에 조항 ID를 넣는다(`feat(R2): 기준점 결정기 — FR-PRG-003`). CP4 교차 리뷰에서 **어느 커밋이 어느 게이트를 건드렸는지** 추적할 수 있어야 한다.

---

## 3. 테스트 규약

### 3.1 계층

| 계층 | 대상 | 실행 시점 |
| --- | --- | --- |
| **G. 게이트 테스트** | 상한 강제 3경로 + 경계값 | 매 세션 (필수) |
| **C. 계약 테스트** | API 응답 형태, 로그 필드 존재 | 매 세션 |
| **U. 단위 테스트** | 순수 함수 (분할, 결정기, 조립) | 매 세션 |
| **R. 리그레션** | 스포일러 30건 + 인젝션 10건 | CP5 · CP6 |

**픽스처는 시드 데이터다.** 공통 계약 3장의 시드에 상한 케이스 3가지가 심어져 있으므로 별도 픽스처를 만들지 않는다.

### 3.2 게이트 테스트는 조항에서 유도한다 — 순서가 중요하다

**구현 전에 테스트를 쓴다.** 구현 후에 쓰면 Claude Code가 코드를 보고 테스트를 만들어, 잘못된 동작을 그대로 통과시킨다.

```
1. 조항 문구를 그대로 테스트 이름에 넣는다
   test("FR-SPL-002: 조회 결과에 기준점 초과 위치 값을 가진 레코드가 0건")

2. 실패하는 것을 확인한다 (구현 전이므로 당연히 실패)

3. 구현한다

4. 통과를 확인한다
```

### 3.3 ⛔ Claude Code에게 명시할 금지

| 금지 | 이유 |
| --- | --- |
| **테스트가 실패하면 테스트를 고치지 않는다. 구현을 고친다** | 가장 흔한 실패 모드다. 특히 상한 테스트에서 assertion을 느슨하게 바꾸는 일이 잦다 |
| **상한 테스트의 assertion을 "대부분" "거의"로 쓰지 않는다** | FR-SPL-002는 **0건**이다. 1건도 통과가 아니다 |
| 게이트 테스트를 skip·주석 처리하지 않는다 | 일정 압박에서 나오는 조치인데, 그 게이트가 데모의 본질이다 |
| 테스트용 우회 경로(상한 무시 플래그)를 만들지 않는다 | 그 경로가 곧 우회 표면이다. FR-SPL-005 🚦 위반 |

### 3.4 negative test가 이 프로젝트의 중심이다

상한 검증은 대부분 **"안 보이는 것"을 확인**하는 테스트다. 조작하기 쉽고, 통과해도 실제로 막혔는지 알기 어렵다.

**대응** — 반드시 쌍으로 쓴다.

```
test("K=40에서 후반부 관계(확립 p.120)가 보이지 않는다")   ← negative
test("K=130에서 같은 관계가 보인다")                        ← positive (쌍)
```

positive가 없으면 "쿼리가 항상 빈 결과를 반환해도" 테스트가 통과한다. 실제로 자주 나오는 버그다.

---

## 4. 인간 검수 체크리스트

코드를 다 읽을 시간은 없다. **아래만 본다.**

### CP2 (8/20 저녁) — 각 파트 15분

| # | 확인 | 방법 |
| --- | --- | --- |
| 1 | 시드의 **후반부 관계가 앞 기준점에서 안 보이는가** | 화면에서 직접 |
| 2 | 같은 관계가 **뒤 기준점에서는 보이는가** | 화면에서 직접 (쌍 확인) |
| 3 | 챗봇이 근거 밖 질문에 **정확히 그 문구로** 거절하는가 | 문자열 완전 일치 |
| 4 | 로그 테이블에 레코드가 **실제로 쌓이는가** | 직접 조회 |

3번은 눈으로 보면 비슷해 보여도 조사 하나가 다를 수 있다. 확정 문구와 문자 단위로 비교한다.

### CP3 (8/21 저녁) — 전원 30분

| # | 확인 |
| --- | --- |
| 1 | 대시보드 → 브리핑 → 읽기 → 싸비 3탭이 한 번에 흐르는가 |
| 2 | **첫 진입 시** 브리핑이 뜨고 "아직 읽은 내용이 없습니다"가 표시되며 **LLM 호출이 0회**인가 |
| 3 | 30분 이내 재진입 시 브리핑을 건너뛰는가 |
| 4 | 대시보드 % · 브리핑 % · 싸비 기준점이 **전부 같은가** |
| 5 | 미완비 도서가 클릭 불가이고, **API를 직접 때려도 거절**되는가 |

2번과 5번은 화면만 보면 통과처럼 보이므로 로그·네트워크 탭을 함께 본다.

### CP4 (8/22) — 교차 리뷰, 파트당 30분

**코드를 다 읽지 않는다. 세 가지만 본다.**

| # | 확인 | 방법 |
| --- | --- | --- |
| 1 | 조회·조립·검색 함수에 **cutoff 인자가 있는가** | 시그니처만 훑는다 |
| 2 | cutoff의 **출처가 기준점 결정기 반환값뿐인가** | 호출부 역추적 |
| 3 | `page - 1` · `/ total` 패턴이 결정기 밖에 있는가 | 전체 검색 |

3번은 grep 한 번이면 끝난다. 결정기 파일 외에서 나오면 위반이다.

**리뷰 결과를 기록한다.** 확인 항목, 위반 건수, 리뷰어, 시각. **이 기록 자체가 NFR-SEC-006 판정 ①의 합격 근거다** — 리뷰를 했어도 기록이 없으면 판정 불가다.

### CP5 (8/25) — 게이트 판정, 반나절

| 판정 | 합격 |
| --- | --- |
| 스포일러 리그레션 30건 | 누설 0건 |
| 인젝션 10건 — 로그의 투입 레코드 + **검색 선정 페이지** 대조 | 초과 레코드 0건 |
| 같은 10건 응답에 기준점 초과 인물·사건 | 0건 |
| 챗봇 오탐 (자동 15 + 탐색 누적) | ≥27/30 |

**미달이면 남은 3일이 수정 여유다.** 이 판정을 8/27로 미루면 고칠 시간이 없다.

### CP6 (8/27 리허설)

성능 3지점 재측정(발표장 네트워크) · 인젝션 재실행 · **같은 질문 반복 실행**(A10 — 1회 통과를 근거로 삼지 않는다) · 시연 위치 리캡 주입 확인.

---

## 5. Claude Code 실패 모드와 대응

이 프로젝트에서 특히 나오기 쉬운 것들이다.

| 실패 모드 | 징후 | 대응 |
| --- | --- | --- |
| **빈칸 채우기** | 미결 항목(난이도 규칙, 모델 배분)을 그럴듯하게 정해버린다 | 세션 프롬프트에 "명세에 없으면 멈추고 질문" 명시. 공통 계약 6장 목록을 알려둔다 |
| **상한을 프롬프트로 옮김** | 쿼리에 필터 대신 시스템 프롬프트에 "기준점 이후를 말하지 마"를 넣는다 | R4 불변식 위반. CP4 리뷰 1번 항목으로 걸린다 |
| **cutoff 인자 누락** | 조회 함수를 `findX(bookId)`로 만든다 | 공통 계약 2.2절을 세션마다 참조. grep으로 확인 |
| **테스트 완화** | 실패하는 assertion을 느슨하게 고친다 | 3.3절 금지. 커밋 diff에서 테스트 파일 변경을 우선 확인 |
| **프론트 재계산** | 편의상 `Math.floor(page/total*100)`을 프론트에 넣는다 | CP4 리뷰 3번(grep)로 걸린다 |
| **로그 누락** | 기능은 되는데 로그 적재를 빠뜨린다 | 세션 완료 조건에 "로그 레코드 생성 확인" 포함 |
| **첫 진입 분기 합침** | `cutoff = 0`을 "저장분 부재"와 같이 처리해 LLM을 부른다 | CP3 검수 2번 |
| **간선 양끝 검증 누락** | 관계도에서 노드 없는 간선이 나온다 | 화면상 티가 안 나므로 테스트로만 잡힌다 |

---

## 6. 시간 배분 — 사람의 검수 예산

4개 스트림이 병렬로 도는 만큼, **검수 자체가 병목이 될 수 있다.**

| 시점 | 사람이 쓰는 시간 |
| --- | --- |
| CP0 | 2시간 (스택 결정 + 계약 동결 + 시드 작성) |
| CP1 | 30분 (실측 보고 → 페이지 크기 판정) |
| CP2 | 파트당 15분 |
| CP3 | 전원 30분 |
| CP4 | 파트당 30분 (교차 리뷰) + 기록 |
| 상시 | R1의 검수 6항목 — **가장 오래 걸린다.** 생성물 전량이 대상이다 |

**R1의 검수가 유일하게 사람 시간을 크게 먹는다.** 나머지는 판정이라 짧다. R1 담당은 8/21 하루를 검수에 통째로 비워두는 편이 안전하다.
