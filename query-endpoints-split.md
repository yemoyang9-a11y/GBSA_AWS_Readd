# 조회 엔드포인트 5종 — 3인 분담과 병합 순서

**브랜치** `feature/R4-query-endpoints`
**정본** `docs/superpowers/plans/2026-08-20-r4-query-endpoints.md` (태스크 8개 전문)
— 이 문서는 그 계획서의 분담·순서 부분만 추린 요약이다. **내용이 갈리면 계획서가 정본이다.**

계약상 R4 소유인 조회 엔드포인트 5개를 R1·R3·R4가 나눠 구현한다.
막히는 건 코드량이 아니라 **같은 파일 상단 12줄**이다. 거기만 줄을 세우면 나머지는 전부 병렬로 간다.

---

## 1. 착수 조건 — 전부 풀렸다

| 항목 | 결정 | 정한 사람 |
| --- | --- | --- |
| **D-1** `/books`의 `total_pages` | **뺀다.** `progress.current_page`와 함께 있으면 프론트가 퍼센트를 재계산할 재료가 된다(절대 규칙 2번). 읽는 컴포넌트가 0개임을 확인했고 프론트 반영도 끝났다(`0348b7e`) | R4 |
| **D-2** `/info`의 `introduction` | `background_and_intro WHERE book_id=$1 AND kind='intro'`. 파이프라인이 `register.ts:141-146`에서 실제로 여기 쓴다 | R1 |
| **D-3** 카드 소개 문구 | `intro_summary: string \| null`을 넣되 **출처는 D-2와 같은 행**이다. 계획서가 전제한 `books.intro_summary`는 001 DDL 15행에 선언만 있고 아무도 채우지 않는 **죽은 컬럼**이라, 그대로 갔으면 카드 소개가 영원히 비어 있었다 | R4 |
| 로컬 DB | `001_content_store.sql` 적용 완료. 다만 게이트 테스트는 fake 리포지토리로 돌리므로 **DB 없이도 개발과 검증이 된다** | — |

**기준선** 백엔드 15 suites / 121 tests · 프론트 28 files / 138 tests · 양쪽 `tsc`·`lint` 0건

---

## 2. 소유권

이 5개는 계약상 R4 소유다. **조회 엔드포인트 5종에 한해 세 사람이 소유권을 공유한다.**
CLAUDE.md 6장("다른 파트의 파일을 수정하지 않는다")의 예외를 이 범위에만 두며, 범위 밖 파일에는 적용되지 않는다.

> ⚠️ **R2의 `reading-state/cutoff.service.ts`는 건드리지 않는다.**
> 다섯 엔드포인트 전부가 `getCutoffSnapshot`을 호출하지만 그 파일은 R2 소유이고, R2는 이번 분담에 없다.
> **호출만 하고 수정하지 않는다.** 변경이 필요하면 R2에게 요청한다.

---

## 3. 분담

| 담당 | 태스크 | 분량 | 배정 근거 |
| --- | --- | --- | --- |
| **R1** | 1. content 모듈 → 4. `/info` | 572줄 | 001 DDL 저자. 컬럼명과 적재 위치를 아는 유일한 사람이고, D-2도 R1이 답했다 |
| **R3** | 6. ssabi 모듈 → 7. `/graph` | 808줄 | 챗봇이 전량 주입(B-1)으로 인물·관계·배경지식을 이미 읽는다. 같은 테이블 도메인이고, 가장 무겁고 게이트에 민감한 구간이다 |
| **R4** | 2. `/books` → 3. 가드 → 5. `/pages` | 683줄 | D-1·D-3으로 `/books` 응답 형태를 정했다. 가드는 대시보드 카드 비활성화와 짝이고, `prev_page`·`next_page`는 R4가 요청해 생긴 필드다 |
| — | 8. `/characters` | 432줄 | **미배정.** 소비하는 화면이 아직 없어 데모에 불필요하다. 먼저 끝난 사람이 가져간다 |

---

## 4. `routes.ts` 병합 순서

**충돌 지점은 핸들러가 아니라 파일 상단이다.** 9~14행 import 블록과 19행 근처 조립 줄에
세 사람이 모두 줄을 추가한다. 핸들러는 각자 독립된 `router.get(...)` 블록이라 서로 부딪히지 않는다.

### Round 0 — 동시, 순서 무관

새 파일만 만든다. `routes.ts`를 건드리지 않으므로 충돌이 없다.

- **R1** `content/repository.ts` — 포트 + Row 타입 (어댑터 없이)
- **R3** `ssabi/repository.ts` — 포트. **모든 메서드가 cutoff 인자를 갖는다**(G1)

> **이 두 파일이 3인 병렬의 전제다.** 포트만 있으면 나머지 사람이 fake 리포지토리로
> 서비스와 게이트 테스트를 먼저 쓸 수 있어 어댑터를 기다리지 않는다.

### Round 1 — 직렬, 한 번에 한 명

`routes.ts` 상단 구역에 들어가는 단계다. 앞사람 커밋이 올라간 것을 확인하고 시작한다.
각 커밋은 **`routes.ts` 변경만 담는다** — 다른 파일과 섞으면 충돌 해결이 어려워진다.

1. **R4 — 미완비 도서 가드**
   `api/book-ready.guard.ts` 신규 + import 1줄 + 4개 핸들러에 배선.
   **가드가 맨 앞인 이유** — 4개 핸들러를 건드리는데 지금은 본문이 `501` 한 줄이라 가장 싸다.
   구현이 끝난 뒤에 넣으면 완성된 핸들러 4개를 전부 다시 고쳐야 한다.
2. **R1 — content 조립** — import 1줄 + `createContentServices(pool)` 1줄
3. **R3 — ssabi 조립** — import 1줄 + `createSsabiServices(pool)` 1줄

### Round 2 — 동시, 순서 무관

각자 자기 핸들러 블록만 교체하므로 서로 부딪히지 않는다.

- **R4** `/books` · `/pages`
- **R1** `/info`
- **R3** `/ssabi/graph`

### Round 3 — 먼저 끝난 사람

- `/ssabi/characters` (Task 8). 데모에 불필요하므로 마지막이다.

---

## 5. 셋 다 지켜야 할 것

**착수 전 CLAUDE.md 2장·3장·4장을 읽는다.**
이 5개의 어려운 점은 코드량이 아니라 **상한을 데이터 선택 단계에서 거는 것**이다(FR-SPL-002 🚦).
조회 함수에 cutoff 인자를 빠뜨리면 스포일러가 새고, 그게 이 프로젝트의 실패 조건이다.
프롬프트나 사후 필터로 막으면 안 된다 — **쿼리 조건**이어야 한다.

**ssabi 리포지토리는 모든 메서드가 cutoff 인자를 갖는다.**
계획서의 제약 G1이다. 인자가 없는 메서드는 그 자체로 우회 표면이 된다.

**게이트 로직은 테스트를 먼저 쓴다.**
구현 후에 쓰면 잘못 건 상한을 그대로 굳힌다. 상한 테스트는 "0건"이고,
negative는 반드시 positive와 쌍으로 쓴다(K=20에서 안 보이고 **K=130에서는 보이는지**).
positive가 없으면 쿼리가 항상 빈 결과를 반환해도 통과한다.

**시드는 만들지 않는다.**
공개 콘텐츠 스토어 쓰기는 파이프라인만 할 수 있다(절대 규칙 5번).
게이트 테스트는 fixture를 물린 fake 리포지토리로 돌리고, 실데이터 검증은 R1 파이프라인이 붙는 CP3에 한다.

**병합 직전 `git fetch && git rebase`.**
팀원들이 자주 push한다. 커밋 형식은 `{type}({파트}): {요약} — {조항 ID}` 그대로, 각자 자기 파트 태그를 쓴다.

---

**함께 볼 것** — `CLAUDE.md`(절대 규칙·불변식 R1~R12) · `handoff-r4.md`(역할 공백 경위, 로컬 전용)
