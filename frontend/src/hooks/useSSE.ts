/**
 * SSE 스트리밍 훅 — 리캡(R2)·챗봇(R3)이 같은 프레임을 쓴다 (backend/SSE_SPEC.md, 8/20 확정).
 *   delta  -> 텍스트 이어붙이기
 *   done   -> 정상 종료
 *   error  -> message 표시, 연결 종료
 * 스트리밍 도중 페이지를 넘겨도 진행 중 스트림은 시작 시점 기준점을 유지한다 (UC-27 A5,
 * R3 8/20 확인 — 프론트가 스트림을 끊지 않아도 된다).
 */
export {};
